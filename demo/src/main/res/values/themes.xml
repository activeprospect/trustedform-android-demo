<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrustedForm" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_blue</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/brand_navbars</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:icon">@color/white</item>
        <item name="toolbarStyle">@style/Toolbar.Primary</item>
        <item name="android:statusBarColor">@color/brand_navbars</item>
        <item name="actionMenuTextColor">@color/blue</item>
        <item name="android:actionMenuTextColor">@color/blue</item>


        <!-- Status bar color. -->
    </style>

    <style name="Theme.TrustedForm.Fullscreen" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

</resources>